{{- if and .Values.global.namespace (ne .Release.Namespace .Values.global.namespace) }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
  labels:
    {{- include "clawd-observability.labels" . | nindent 4 }}
{{- end }}
