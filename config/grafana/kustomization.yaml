apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - prometheus-rule-cost-alerts.yaml

configMapGenerator:
  - name: agentic-grafana-dashboards
    files:
      - dashboards/cost-by-workload.json

generatorOptions:
  disableNameSuffixHash: true
